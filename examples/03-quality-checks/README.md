# Example: Quality Checks

Demonstrates all built-in quality check types: not_null, unique, row_count,
accepted_values, and expression. Also shows fill_null, cast, and dedup transforms.

## Run

```bash
cd examples/03-quality-checks
quicketl run pipeline.yml
```
